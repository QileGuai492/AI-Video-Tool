# 29 - 白模视频升级方案

> 状态：待评审  
> 最近更新：2026-08-21  
> 所属文档库：[README](../README.md)

## 1. 背景

“白模视频”（3D 灰盒预演 / Greybox / Blockout / Previs）是目前 AI 视频生成从“抽卡”走向“可控生产”的关键技术。

核心思路：

1. 用低精度 3D 模型（方块、球体、灰模人形）搭建场景、角色站位、动作路径、相机运镜。
2. 导出白模视频。
3. 将白模视频 + 角色参考图 + 场景参考图交给支持视频参考的 AI 视频模型。
4. AI 保留白模的运镜、动作、站位和时序，重建最终材质、光影、角色外观与环境。

参考来源：

- [Seedance 2.5 白模渲染：从灰盒预演到成片](https://pixo.video/zh/blog/seedance-previz-blockout)
- [Seedance 2.5 White-model storyboard](https://github.com/allenGKC/Seedance-2.5/blob/main/skill/seedance-25/references/white-model-storyboard.md)
- [Seedance 2.5 reference (ByteDance)](https://github.com/smixs/visual-skills/blob/main/video/references/seedance-25.md)
- [Seedance ShotDesign Skills - Industrial Workflows](https://github.com/woodfantasy/Seedance-ShotDesign-Skills/blob/main/references/industrial-workflows.md)
- [3D 白模预演 · 一句话直出运镜可控的 CG 大片](https://www.seeles.ai/features/create/greybox-previz/zh-cn)

## 2. 已确认的方向

根据产品讨论，确定以下方向：

1. **白模模块形态**：内置 Web 端 3D 灰模编辑器 + Blender 高级导出。
2. **白模生成方式**：预设模板库 + 自动从文字生成白模；纯手动搭建作为模板库的底层能力。
3. **视频模型策略**：先做 Provider 抽象，优先接入支持“白模视频参考”的模型；当前 SiliconFlow 作为降级通道。
4. **前端方向**：直接开始 React + Three.js 正式前端。
5. **用户复杂度**：兼顾简单模式与高级模式。

## 3. 视频模型调研结论

### 3.1 推荐首选：Seedance 2.5（即梦）

Seedance 2.5 是目前对“白模预演”支持最直接的生产级模型：

| 能力 | Seedance 2.5 |
| --- | --- |
| 白模视频参考 | ✅ 官方支持 3D 白模 blockout 控制 |
| 视频参考数量 | 10 个视频参考，单段 2~30s，总时长 ≤30s |
| 图片参考数量 | 30 张 |
| 音频参考数量 | 10 个 |
| 单次生成时长 | 最长 30s，可扩展 |
| 时间戳控制 | 支持秒级时间戳 |
| 多镜头 | 支持多镜头/多宫格分镜 |
| Blender/Maya 插件 | 官方生态支持，可直传即梦 |

结论：**白模升级首选接入 Seedance 2.5 API 或即梦开放平台。**

### 3.2 备选模型

| 模型 | 特点 | 适合场景 |
| --- | --- | --- |
| Kling / 可灵 | 国内可用，支持图生视频、运镜控制 | 单镜头图生视频 |
| PixVerse C1 | 面向影视制作，强调分镜/工业流程 | 影视级分镜 |
| Luma Scenes / Ray | Storyboard-first，支持场景控制 | 分镜到视频 |
| Runway / Veo / Sora | 世界模型方向，物理一致性更强 | 长期演进 |

### 3.3 世界模型（World Model）趋势

世界模型的目标是让模型理解并模拟物理世界，而不只是“生成看起来像视频的像素”。

代表方向：

- OpenAI Sora：把视频生成定义为“世界模拟器”。
- Google Genie / Veo：可交互世界模型 + 视频生成。
- 国内多家厂商也在向“世界模型”演进。

白模预演与世界模型是互补关系：

- 白模提供结构控制。
- 世界模型提供物理一致性和长期动作推理。
- 未来可以把“白模场景”直接作为世界模型的初始状态输入。

本项目应保持 Provider 抽象，为世界模型类接口预留扩展点。

### 3.4 降级方案（当前 SiliconFlow）

如果暂时不接入 Seedance 2.5，可用当前 SiliconFlow 完成降级版白模流程：

```text
白模视频 → 抽关键帧 → 图生视频（每镜头） → 分镜拼接
```

优点：不改当前 Provider。
缺点：运镜/动作连续性和角色一致性弱于视频参考方案。

## 4. 总体架构

```text
[React + Three.js 前端]
        │
        ├── 白模编辑器（简单模式 / 高级模式）
        ├── 分镜时间轴
        └── 任务中心
                │
                ▼
[FastAPI 后端]
        │
        ├── PrevisService
        │     ├── 模板库
        │     ├── 文字→白模（LLM + 场景脚本）
        │     ├── Three.js 场景 JSON
        │     └── Blender 渲染服务（可选）
        │
        ├── VideoProvider 抽象
        │     ├── Seedance 2.5（首选）
        │     ├── SiliconFlow（降级）
        │     └── 未来世界模型
        │
        └── Storage（本地 / OSS）
```

## 5. 数据模型设计

### 5.1 VideoTask 扩展

```text
previs_video_url     # 白模/灰盒预演视频地址
previs_type          # none / coarse / fine
previs_scene_json    # Three.js / Blender 场景 JSON
storyboard_url       # 多宫格分镜图地址
camera_script        # 镜头脚本 JSON
reference_video_url  # 给视频模型使用的参考视频
```

### 5.2 新增 PrevisProject

```text
PrevisProject
├── id
├── user_id
├── title
├── mode               # template / auto / manual
├── template_id        # 使用的模板
├── scene_json         # 场景数据
├── previs_video_url   # 渲染出的白模视频
├── mapping_rules      # 白模对象 -> 最终角色/道具/场景映射
├── status             # draft / rendering / ready / failed
└── created_at
```

### 5.3 新增 PrevisTemplate

```text
PrevisTemplate
├── id
├── name
├── description
├── thumbnail_url
├── scene_json          # 模板场景
├── category            # 人物 / 产品 / 场景 / 运镜
├── is_builtin
└── created_at
```

## 6. API 设计

```text
# 模板
GET    /api/v1/previs/templates
POST   /api/v1/previs/templates          # 保存自定义模板

# 白模项目
POST   /api/v1/previs/projects           # 创建白模项目
GET    /api/v1/previs/projects/{id}
PUT    /api/v1/previs/projects/{id}      # 保存场景/镜头脚本
POST   /api/v1/previs/projects/{id}/render   # 渲染白模视频

# 自动生成
POST   /api/v1/previs/generate           # 文字 → 白模项目

# 生成任务
POST   /api/v1/generate/video            # 扩展支持 reference_video_url / previs_video_url
```

## 7. 白模生成链路

### 7.1 预设模板库

内置模板：

- 人物行走/奔跑模板
- 双人对话模板
- 产品展示运镜模板
- 场景空镜运镜模板
- 追逐/动作模板
- 多镜头分镜模板

用户选择模板后进入编辑器微调。

### 7.2 自动从文字生成白模

流程：

```text
用户文案
  → LLM 生成镜头脚本（镜头数、时长、角色、动作、运镜）
  → 场景脚本（布局、物体、角色位置）
  → 生成 Three.js 场景 JSON
  → 前端渲染预览
  → 用户确认/微调
  → 渲染白模视频
```

### 7.3 纯手动搭建（高级模式）

- 拖拽添加方块/圆柱/球体/灰模人形。
- 设置角色站位、动作关键帧。
- 设置相机路径与运镜。
- 导出场景 JSON / 白模视频。
- 可导出到 Blender 精修。

### 7.4 Blender 高级导出

- 提供 Blender Add-on 或独立脚本。
- 支持导入 Three.js 场景 JSON。
- 在 Blender 中精修动作、镜头、灯光。
- 渲染白模视频并回传平台。

## 8. 提示词模板

### 8.1 粗白模模板

```text
参考@视频1的运镜、动作路径、角色站位与镜头节奏。
将其中[蓝色人形]映射为@图片1的[女主]；将[橙色人形]映射为@图片2的[男主]。
[描述连续动作、表情、对话与光线变化。]
场景使用@图片3的[环境]。
全程保持角色身份、比例、站位和动作连续；不保留白模材质、轨迹线、坐标线或相机锥体。
```

### 8.2 细白模模板

```text
将@视频1的细颗粒白模渲染为最终成片。
几何结构、人物动画、机位和切镜时序不变。
[按时间点描述材质、灯光、环境、特效变化。]
去除全部坐标线、轨迹线、相机锥体、视口网格和调试文字。
```

### 8.3 多宫格分镜模板

```text
@图片1是按左到右、上到下排列的[N]格连续分镜，每格代表一个完整镜头。
角色A参考@图片2，角色B参考@图片3，场景参考@图片4。
镜头1（0–Xs）：[构图、景别、运镜、动作、落幅]。
镜头2（X–Ys）：[承接、运镜、动作、落幅]。
全片保持人物、服装、比例、道具、光线方向与空间连续。
```

## 9. 编排流水线

当前：

```text
PromptAgent → ImageAgent → VideoGenAgent → Audio/Subtitle → PostProcess
```

升级后：

```text
PromptAgent → PrevisAgent → ImageAgent → VideoGenAgent(参考白模) → Audio/Subtitle → PostProcess
```

新增 `PrevisAgent`：

1. 根据文案生成镜头脚本。
2. 选择/生成白模场景。
3. 调用渲染服务生成白模视频。
4. 生成“白模对象 → 最终角色/道具/场景”映射提示词。
5. 交给 VideoGenAgent。

## 10. 实施路线

| 阶段 | 内容 | 优先级 |
| --- | --- | --- |
| Phase 1 | 数据模型、Provider 抽象、Seedance 2.5 接入评估 | P0 |
| Phase 2 | React + Three.js 白模编辑器（模板库 + 手动搭建） | P0 |
| Phase 3 | 文字→白模自动生成（LLM + 场景脚本） | P1 |
| Phase 4 | Blender 高级导出/渲染服务 | P2 |
| Phase 5 | 世界模型/更多模型接入 | P2 |

## 11. 风险与应对

| 风险 | 应对 |
| --- | --- |
| Seedance 2.5 API 尚未开放/成本高 | Provider 抽象，先 SiliconFlow 降级 |
| 3D 编辑器开发量大 | 先做模板库 + 基础灰模编辑，再逐步增强 |
| 文字→白模生成不稳定 | 先支持结构化模板参数，再开放自由生成 |
| Blender 服务部署复杂 | 先提供本地导出脚本，再容器化 |
| 白模到成片映射不稳定 | 沉淀提示词模板 + 评测用例持续回归 |

## 12. 验收标准

- 用户可以从模板或文字创建白模项目。
- 白模项目可以渲染为白模视频。
- 提交生成任务时可以选择参考白模视频。
- 成片保留白模的运镜、动作和空间关系。
- 高级用户可以将场景导出到 Blender。
- 前端提供简单模式与高级模式。
