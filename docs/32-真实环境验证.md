# 32 - 真实环境验证

> 状态：待账户开通后执行  
> 最近更新：2026-08-21  
> 所属文档库：[README](../README.md)

## 1. 验证目标

验证白模降级流水线在真实 SiliconFlow 环境下可用：

```text
白模视频 → 按镜头抽帧 → 逐镜头图生视频 → 拼接成片
```

## 2. 前置条件

- `.env` 已配置：
  ```env
  SILICONFLOW_API_KEY=your_key
  ```
- SiliconFlow 账户有余额/模型权限。
- 本地或容器可访问 SiliconFlow API。

## 3. 执行验证

```bash
python scripts/validate_real_previs.py
```

## 4. 预期结果

- `register 200`
- `submit 200`
- 状态逐步推进：
  ```text
  optimizing_prompt → generating_first_frame → generating_video → completed
  ```
- 最终输出：
  ```text
  FINAL {'status': 'completed', 'progress': 1.0, ...}
  ```

## 5. 已知阻塞

当前 SiliconFlow 返回：

```text
402 Payment Required
```

表示账户余额不足或未开通对应模型权限。需要在 SiliconFlow 平台充值/开通后重新执行。

## 6. 验证通过标准

- 任务状态为 `completed`
- `video_url` 非空
- 下载接口可返回真实视频文件
