global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/prometheus-alerts.yml

scrape_configs:
  - job_name: ai-video-api
    metrics_path: /api/v1/metrics/prometheus
    static_configs:
      - targets: ["api:8000"]
